p.
  This object wraps content, limiting its maximum width.
  Use it to wrap content directly inside a Runner
  (though it may not necessarily be a child of Runner).
figure.docs-figure
  div.o-Runner
    div.o-Constrain
      h1 content heading
      p content
  pre.docs-pre.
    &lt;div class="#[u o-Runner]"&gt;
      &lt;div class="#[u o-Constrain]"&gt;
        &lt;h1&gt;content heading&lt;/h1&gt;
        &lt;p&gt;content&lt;/p&gt;
      &lt;/div&gt;
    &lt;/div&gt;
//- p.
//-   This Object also may not necessarily be a wrapper #[i(lang="la") per se];
//-   it may be applied to a single element like a heading.
//- figure.docs-figure
//-   div.o-Runner
//-     h1.o-Constrain heading with a max-width
//-   pre.docs-pre.
//-     &lt;div class="#[u o-Runner]"&gt;
//-       &lt;h1 class="#[u o-Constrain]"&gt;heading with a max-width&lt;/h1&gt;
//-     &lt;/div&gt;
