h1#registration Registra&shy;tion
!= Util.view(site.currentConference.getPassesAll()).pass(site.currentConference, site.getQueue('Featured Passes'))
-
  let highlight_buttons = [
    ElemName('a').attr('href','registration.html').textContent(`Registration Details`)
  ]
!= Util.view(highlight_buttons).highlightButtons()
