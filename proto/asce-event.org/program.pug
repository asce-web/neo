extends ../default/program.pug

block mainMinimal
  include _includes/_text-block.pug

block callouts
  +callout().c-Banner.c-Banner--blur(style=`--banner-img: url('${site.currentConference.promoLocImage}');`)
    include _includes/callouts/_location.pug
  +callout().c-Banner
    include _includes/callouts/_volunteer.pug
